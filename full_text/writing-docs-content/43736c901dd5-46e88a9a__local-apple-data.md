---
name: local-apple-data
description: Use when a user asks Codex to search or inspect locally synced Mail.app mail, Messages chats, inferred Hide My Email aliases, Voice Memos, Safari bookmarks/Reading List, Shortcuts metadata, Apple Books metadata/annotations, Apple Podcasts metadata, Apple Music metadata, Apple TV metadata, Apple Freeform metadata, Apple Notes, Apple Calendar, Apple Contacts, Apple Photos, Apple Reminders, or iCloud Drive files without the Gmail connector. The skill uses the local MCP server, stays metadata-first, retrieves or exports content/detail only by exact opaque handle for approved local surfaces, and allows only approved Reminders, iCloud Drive, Calendar, Contacts, Notes, Mail draft, Photos import, or Messages send-text apply after plan-token confirmation.
---

# Local Apple Data

Use the `local-apple-data` MCP server for the user's locally synced Mail.app mail, Messages chats, inferred Hide My Email aliases, Voice Memos, Safari bookmarks/Reading List, Shortcuts metadata, Apple Books metadata/annotations, Apple Podcasts metadata, Apple Music metadata, Apple TV metadata, Apple Freeform metadata, Apple Notes, Apple Calendar, Apple Contacts, Apple Photos, Apple Reminders, and iCloud Drive files. Do not use the Gmail connector, Gmail API, IMAP, OAuth, app passwords, iCloud.com, browser sessions, keychain credentials, private iCloud web APIs, or network mail access for these workflows unless the user explicitly asks for those external paths.

## Workflow

1. Start with `apple_data_health` or `apple_data_doctor` if current local readiness has not been checked in this session.
2. Use narrow, user-requested searches only. Do not run broad dumps or empty searches.
3. Keep limits small unless the user asks for a larger result set.
4. Treat search results as local metadata that may include subjects, Messages chat display names, inferred Hide My Email alias previews, Voice Memo titles, Safari bookmark titles/domains, Shortcuts names, Books titles/authors/genres, Podcasts show titles/authors/categories/providers, Music track/artist/album/genre and playlist names, TV item/show/artist/genre and playlist names, Freeform board recency/count metadata and folder titles, note titles/snippets, calendar event titles, contact names/organizations, Photos filenames, iCloud Drive filenames, or reminder titles. Summarize only the minimum useful facts in chat.
5. Use exact opaque handles from search results for metadata fetches. Do not fabricate handles or retry old raw row-ID handles.
6. If the user asks for Mail message content and you are selecting from search results, prefer a result whose `content_status` is `available`. Treat `unavailable` or `unknown` as a reason to skip that handle or report that local content may not be retrievable.
7. Call `mail_get_content` only on an exact `mail:message:v2:` handle the user selected, provided, or approved from the metadata results. Keep `max_chars` bounded; use 4000 unless the user asks for a smaller limit, and never above 12000.
8. Call `mail_list_attachments` only on an exact `mail:message:v2:` handle the user selected, provided, or approved from Mail metadata results. Use `mail_export_attachment` only when the user asks to export one selected attachment, provide both the original message handle and selected `mail:attachment:v1:` handle, and provide a caller-selected output directory. Do not request broad Mail attachment exports, inline attachment bytes, source message paths, raw MIME/full headers, externalized attachment fetches, or attachment mutation.
9. Call `messages_get_chat` only on an exact `messages:chat:v1:` handle the user selected, provided, or approved from Messages chat display-name metadata results. Keep `max_messages` and `max_chars` bounded. It may return bounded plaintext from local `attributedBody` fallback rows, but do not request or expose raw typedstream blobs, attributed-string attributes, participant identifiers, or broad message-text search.
10. Call `messages_list_attachments` only on an exact `messages:chat:v1:` handle the user selected, provided, or approved from Messages chat metadata results. Use `messages_export_attachment` only when the user asks to export one selected attachment, provide both the original chat handle and selected `messages:attachment:v1:` handle, and provide a caller-selected output directory. Do not request broad Messages attachment exports, inline attachment bytes, source media paths, unavailable iCloud media fetches, participant identifiers, or attachment mutation.
11. Call `hide_my_email_get_alias` only on an exact `hide_my_email:alias:v1:` handle the user selected, provided, or approved from inferred Hide My Email metadata results. Treat it as inferred local Mail evidence, not authoritative iCloud inventory. Do not use iCloud.com, browser sessions, keychain credentials, private iCloud web APIs, or network services for Hide My Email.
12. Call `voice_memos_get_recording` only on an exact `voice_memos:recording:v1:` handle the user selected, provided, or approved from Voice Memos title/filename metadata results. Keep `max_chars` bounded; use 4000 unless the user asks for a smaller limit, and never above 12000. This may return an existing Apple-generated transcript only; it does not return audio bytes or generate new transcription. Use `voice_memos_export_audio` only when the user asks to export the selected recording, and provide a caller-selected output directory.
13. Call `safari_get_item` only on an exact `safari:item:v1:` handle the user selected, provided, or approved from Safari bookmark/Reading List metadata results. Search results return title/domain metadata only, not full URLs. Do not request Safari history, open tabs, private browsing data, passwords, cookies, browser caches, page content, broad bookmark dumps, or bookmark mutation.
14. Call `shortcuts_get_item` only on an exact `shortcuts:item:v1:` handle the user selected, provided, or approved from Shortcuts name metadata results. Search results return shortcut/folder names only, not raw identifiers or shortcut bodies. Do not request shortcut run/open/view/sign/export, body/action graph reads, dynamic run tools, folder-scoped handles, Shortcuts SQLite scraping, or mutation.
15. Call `books_get` only on an exact `books:book:v1:` handle the user selected, provided, or approved from Books title/author/genre metadata results. Use `books_list_annotations` only when the user asks for annotations from that selected book. Search results return book metadata and annotation counts only, not annotation text, book text, raw asset IDs, annotation UUIDs, or local paths. Do not request broad library dumps, broad annotation searches/dumps, book/chapter/PDF/EPUB text extraction, iCloud content fetches, Books.app automation, or Books mutation.
16. Call `podcasts_get_show` only on an exact `podcasts:show:v1:` handle the user selected, provided, or approved from Podcasts show metadata results. Use `podcasts_list_episodes` only when the user asks for episodes from that selected show, and use `podcasts_get_episode` only on an exact `podcasts:episode:v1:` handle from that selected-show episode list. Search and episode-list results do not return episode descriptions, transcript text, audio bytes, feed/enclosure URLs, raw identifiers, or local download paths.
17. Call `music_get_track` only on an exact `music:track:v1:` handle the user selected, provided, or approved from Music track metadata results. Call `music_get_playlist` only on an exact `music:playlist:v1:` handle from Music playlist metadata results. Call `tv_get_item` only on an exact `tv:item:v1:` handle from TV item metadata results. Call `tv_get_playlist` only on an exact `tv:playlist:v1:` handle from TV playlist metadata results. Call `freeform_get_board` only on an exact `freeform:board:v1:` handle from Freeform board metadata output. Call `freeform_get_folder` only on an exact `freeform:folder:v1:` handle from Freeform folder metadata output. Search/list results return Music/TV/Freeform metadata only, not audio/video bytes, lyrics, file paths, artwork, descriptions, board titles/content, board BLOBs, decoded board items, assets, previews, collaboration payloads, raw identifiers, play history, playback/watched state, ratings/favorites, playlist item dumps, or Music/TV/Freeform mutation.
18. Call `notes_get_content` only on an exact `notes:note:v2:` handle the user selected, provided, or approved from the metadata results. Keep `max_chars` bounded; use 4000 unless the user asks for a smaller limit, never above 12000, and follow `next_offset` when the user asks for a full long/imported note.
19. Call `notes_list_attachments` only on an exact `notes:note:v2:` handle the user selected, provided, or approved from Notes metadata results. Use `notes_export_attachment` only when the user asks to export one selected attachment, and provide a caller-selected output directory. Do not request broad attachment exports, inline attachment bytes, source media paths, or remote attachment fetches.
20. Call `icloud_drive_get_content` only on an exact `icloud:file:v1:` handle the user selected, provided, or approved from filename metadata results. Keep `max_chars` bounded; use 4000 unless the user asks for a smaller limit, and never above 12000.
21. Call `calendar_get_event` only on an exact `calendar:event:v1:` handle the user selected, provided, or approved from event-title metadata results. Keep `max_chars` bounded; use 4000 unless the user asks for a smaller limit, and never above 12000.
22. Call `contacts_get` only on an exact `contacts:contact:v1:` handle the user selected, provided, or approved from Contacts name/organization metadata results. Keep `max_chars` bounded; use 4000 unless the user asks for a smaller limit, and never above 12000.
23. Call `photos_get_asset` only on an exact `photos:asset:v1:` handle the user selected, provided, or approved from Photos original-filename metadata results. This returns asset/resource metadata only. Use `photos_export_asset` only when the user asks to export the selected asset, and provide a caller-selected output directory.
24. Call `reminders_get_content` only on an exact `reminders:reminder:eventkit:v1:` handle the user selected, provided, or approved from EventKit reminder-title metadata results. Keep `max_chars` bounded; use 4000 unless the user asks for a smaller limit, and never above 12000.
25. Call `reminders_plan_change` only when the user asks to plan a future Reminder create, complete, or due-date update. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true`, does not call EventKit, and does not modify Reminders.
26. Call `reminders_apply_change` only after a matching Reminders plan, an exact `reminders-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, and operation-specific expected state.
27. Call `icloud_drive_plan_change` only when the user asks to plan a future iCloud Drive text-file create or append-text operation. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true` and does not modify iCloud Drive.
28. Call `icloud_drive_apply_change` only after a matching iCloud Drive plan, an exact `icloud-drive-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, and either the same parent handle/filename/content for create or the same file handle/expected current SHA-256/content for append.
29. Call `calendar_plan_change` only when the user asks to plan a future Calendar timed-event create in an explicit target calendar. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true`, does not call EventKit, and does not modify Calendar data.
30. Call `calendar_apply_change` only after a matching Calendar plan, an exact `calendar-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, and the same calendar title, title, start date, end date, location, and notes. It may only create one timed event in the explicit target calendar.
31. Call `contacts_plan_change` only when the user asks to plan a future Contacts contact create with bounded person or organization fields. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true`, does not call Contacts.framework, and does not modify Contacts data.
32. Call `contacts_apply_change` only after a matching Contacts plan, an exact `contacts-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, and the same contact type, name/organization fields, and labeled email/phone/URL values. It may only create one contact through Contacts.framework.
33. Call `notes_plan_change` only when the user asks to plan a future Notes create or append-text operation. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true`, does not call Notes.app, and does not modify Notes data.
34. Call `notes_apply_change` only after a matching Notes plan, an exact `notes-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, and either the same title/body for create or the same note handle/expected current SHA-256/body for append. It may only create one plaintext note or append bounded plaintext to one exact note through Notes.app automation.
35. Call `mail_plan_change` only when the user asks to plan a future Mail draft create with bounded recipients, subject, and body text. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true`, does not call Mail.app, and does not modify Mail data.
36. Call `mail_apply_change` only after a matching Mail plan, an exact `mail-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, and the same recipients, subject, and body text. It may only save one plaintext draft through Mail.app automation and must not send mail.
37. Call `photos_plan_change` only when the user asks to plan a future Photos image/video import from a caller-selected local source file. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true`, does not call PhotoKit, does not modify Photos, and does not echo the raw source path.
38. Call `photos_apply_change` only after a matching Photos plan, an exact `photos-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, and the same source file. It may only import one local image or video through PhotoKit and must not edit, delete, target albums, mutate metadata, or fetch iCloud media over the network.
39. Call `messages_plan_change` only when the user asks to plan a future Messages send-text operation to one exact existing chat. This is non-mutating: it returns `mutation_applied:false` and `apply_available:true`, does not call Messages.app, does not modify Messages data, and returns only bounded body preview plus approval metadata.
40. Call `messages_apply_change` only after a matching Messages plan, an exact `messages-apply:v1:<approval_fingerprint>` approval token, explicit user approval for that exact operation, the same exact `messages:chat:v1:` handle, and the same body text. It may only send bounded plaintext to one existing chat through Messages.app automation, must confirm local `chat.db` read-back, must detect ghost rows, and must not send to direct recipients, create chats, choose SMS fallback, send files, react, edit, delete, or echo the sent body in apply output.
41. If the user asks for broad attachment export, inline attachment bytes, broad Messages attachment export, Reminders/Voice Memos attachments, broad content search, Gmail fallback, broad Calendar/Contacts/Photos/Reminder dumps, broad Messages text search, broad Voice Memos transcript search, Safari history/open-tabs/passwords/cookies/browser-cache/page-content/bookmark mutation, Shortcuts run/open/view/sign/export/body/action-graph/mutation, Books book/chapter/PDF/EPUB text extraction, broad library dumps, broad annotation search/dumps, Books raw IDs/paths, Books iCloud fetch, Books.app automation or mutation, Podcasts transcript/audio/feed/enclosure URL extraction, broad Podcasts library or episode-description dumps/searches, Podcasts raw IDs/paths, Podcasts iCloud media fetch, Podcasts.app automation or mutation, Music audio/lyrics/file-path export, broad Music library dumps/searches, Music raw IDs/paths, raw Music database parsing, play history, ratings/favorites, playlist track dumps, iCloud media fetch, playback/queue control, Music.app mutation, TV video/file-path/artwork/description export, broad TV library dumps/searches, TV raw IDs/paths, raw TV library parsing, watched/playback state, playlist item dumps, TV.app mutation, Freeform board BLOB/CRDT decoding, board title/content export, decoded board item dumps, asset export, previews, collaboration payloads, broad Freeform dumps/searches, raw Freeform rows/identifiers, Freeform.app automation or mutation, authoritative Hide My Email inventory, Hide My Email creation/deactivation/deletion, private iCloud web/API access, browser/keychain credential access, generated Voice Memos transcription, Contact update/delete/merge/move/group membership/postal-address/birthday/relationship/social-profile/notes/image mutation, Notes arbitrary update/delete/move/folder-account/rich-text/attachment mutation, locked/shared-note mutation, Mail send/reply/forward/archive/move/delete/mark/flag/mailbox-account mutation, Mail attachment mutation, Messages direct-recipient send/new-chat/SMS-fallback selection/file-send/edit/delete/reaction/tapback or attachment mutation, Photos edit/delete/album/hidden/favorite/metadata mutation, arbitrary document/binary extraction, unsupported UI automation, iCloud Drive overwrite/delete/binary/document writes, Calendar update/delete/recurrence/attendees/alarms/all-day/default-calendar guessing, or mutation other than approved Reminders create/complete/due-date apply, iCloud Drive create/append-text apply, Calendar create-event apply, Contacts create-contact apply, Notes create/append-text apply, Mail create-draft apply, Photos import apply, or Messages send-text apply, stop and explain that those are outside this plugin's approved surface.

## Tools

- `apple_data_health`: redacted readiness and schema-only local checks.
- `apple_data_doctor`: redacted diagnostics and remediation guidance.
- `mail_search`: capped subject-metadata search across local Mail.app stores, with a metadata-only `content_status` hint.
- `mail_get_metadata`: exact Mail metadata by opaque handle.
- `mail_get_content`: exact Mail plain-text content by opaque `mail:message:v2:` handle, capped and read-only.
- `mail_list_attachments`: exact selected-message attachment metadata by opaque `mail:message:v2:` handle, capped and read-only.
- `mail_export_attachment`: exact Mail attachment export by original `mail:message:v2:` handle plus opaque `mail:attachment:v1:` handle to a caller-selected output directory, read-only.
- `mail_plan_change`: non-mutating future Mail draft create preview, with idempotency and approval metadata only.
- `mail_apply_change`: approved Mail draft create, requiring a matching plan approval token, explicit confirmation, save-only Mail.app automation, and local Drafts read-back verification when available.
- `messages_search`: capped Messages chat display-name metadata search.
- `messages_get_chat`: exact Messages chat transcript by opaque `messages:chat:v1:` handle, capped, read-only, and able to use local `attributedBody` plaintext fallback when `message.text` is empty.
- `messages_list_attachments`: exact selected-chat attachment metadata by opaque `messages:chat:v1:` handle, capped and read-only.
- `messages_export_attachment`: exact Messages attachment export by original `messages:chat:v1:` handle plus opaque `messages:attachment:v1:` handle to a caller-selected output directory, read-only.
- `messages_plan_change`: non-mutating future Messages send-text preview, with exact chat-state, idempotency, bounded body preview, and approval metadata only.
- `messages_apply_change`: approved Messages send-text to one exact existing chat, requiring a matching plan approval token, explicit confirmation, Messages.app automation, ghost-row detection, and local `chat.db` read-back verification.
- `hide_my_email_search`: capped inferred Hide My Email alias search from local Mail address metadata; masked alias previews only.
- `hide_my_email_get_alias`: exact inferred alias detail by opaque `hide_my_email:alias:v1:` handle, capped and read-only, with provenance and `authoritative_inventory:false`.
- `voice_memos_search`: capped Voice Memos title/filename metadata search.
- `voice_memos_get_recording`: exact Voice Memos metadata and existing embedded transcript by opaque `voice_memos:recording:v1:` handle, capped and read-only.
- `voice_memos_export_audio`: exact Voice Memos `.m4a` export by opaque `voice_memos:recording:v1:` handle to a caller-selected output directory, read-only.
- `safari_search`: capped Safari bookmark and Reading List title/URL metadata search; search returns title/domain metadata and not full URLs.
- `safari_get_item`: exact Safari bookmark or Reading List URL detail by opaque `safari:item:v1:` handle, capped and read-only.
- `shortcuts_search`: capped Apple Shortcuts shortcut/folder name metadata search; search returns names and not raw identifiers or shortcut bodies.
- `shortcuts_get_item`: exact Apple Shortcuts metadata by opaque `shortcuts:item:v1:` handle, capped and read-only.
- `books_search`: capped Apple Books title/author/genre metadata search; search returns book metadata and annotation counts, not annotation text, book text, raw identifiers, or local paths.
- `books_get`: exact Apple Books metadata by opaque `books:book:v1:` handle, capped and read-only.
- `books_list_annotations`: exact selected-book annotations by opaque `books:book:v1:` handle, capped and read-only.
- `podcasts_search`: capped Apple Podcasts show title/author/category/provider metadata search; search returns show metadata only, not episode descriptions, transcripts, raw identifiers, URLs, or local paths.
- `podcasts_get_show`: exact Apple Podcasts show metadata by opaque `podcasts:show:v1:` handle, capped and read-only.
- `podcasts_list_episodes`: exact selected-show episode metadata by opaque `podcasts:show:v1:` handle, capped and read-only.
- `podcasts_get_episode`: exact Apple Podcasts episode metadata and bounded description by opaque `podcasts:episode:v1:` handle, capped and read-only; transcripts and audio bytes are not returned.
- `music_search`: capped Apple Music track title/artist/album/genre metadata search; search returns track metadata only, not audio, lyrics, paths, raw identifiers, play history, or ratings.
- `music_get_track`: exact Apple Music track metadata by opaque `music:track:v1:` handle, capped and read-only.
- `music_search_playlists`: capped Apple Music playlist metadata search; search returns playlist metadata only, not playlist tracks or raw identifiers.
- `music_get_playlist`: exact Apple Music playlist metadata by opaque `music:playlist:v1:` handle, capped and read-only.
- `tv_search`: capped Apple TV item title/show/artist/genre/kind metadata search; search returns item metadata only, not video, paths, artwork, descriptions, raw identifiers, playback state, watched state, or ratings.
- `tv_get_item`: exact Apple TV item metadata by opaque `tv:item:v1:` handle, capped and read-only.
- `tv_search_playlists`: capped Apple TV playlist metadata search; search returns playlist metadata only, not playlist items or raw identifiers.
- `tv_get_playlist`: exact Apple TV playlist metadata by opaque `tv:playlist:v1:` handle, capped and read-only.
- `freeform_list_boards`: capped Apple Freeform recent-board metadata list; output excludes board title/content, BLOBs, decoded board items, assets, previews, collaboration payloads, and raw identifiers.
- `freeform_get_board`: exact Apple Freeform board metadata by opaque `freeform:board:v1:` handle, capped and read-only.
- `freeform_search_folders`: capped Apple Freeform folder title metadata search.
- `freeform_get_folder`: exact Apple Freeform folder metadata by opaque `freeform:folder:v1:` handle, capped and read-only.
- `notes_search`: capped Notes title/snippet metadata search.
- `notes_get_metadata`: exact Notes metadata by opaque handle.
- `notes_get_content`: exact Notes plain-text content by opaque `notes:note:v2:` handle, capped, paged, and read-only.
- `notes_list_attachments`: exact selected-note attachment metadata by opaque `notes:note:v2:` handle, capped and read-only.
- `notes_export_attachment`: exact Notes attachment export by opaque `notes:attachment:v1:` handle to a caller-selected output directory, read-only.
- `notes_plan_change`: non-mutating future Notes note create or append-text preview, with idempotency and approval metadata only.
- `notes_apply_change`: approved Notes note create or append-text, requiring a matching plan approval token, explicit confirmation, Notes.app automation, and exact-content read-back verification.
- `icloud_drive_search`: capped iCloud Drive filename metadata search.
- `icloud_drive_get_metadata`: exact iCloud Drive metadata by opaque handle.
- `icloud_drive_get_content`: exact iCloud Drive text-file content by opaque `icloud:file:v1:` handle, capped and read-only.
- `icloud_drive_plan_change`: non-mutating future iCloud Drive text-file create preview, with idempotency and approval metadata only.
- `icloud_drive_apply_change`: approved iCloud Drive text-file create, requiring a matching plan approval token, explicit confirmation, exact parent folder handle, exclusive create, and read-back verification.
- `calendar_search`: capped Calendar event title metadata search through EventKit.
- `calendar_get_event`: exact Calendar event details by opaque `calendar:event:v1:` handle, capped and read-only.
- `calendar_plan_change`: non-mutating future Calendar timed-event create preview, with idempotency and approval metadata only.
- `calendar_apply_change`: approved Calendar timed-event create, requiring a matching plan approval token, explicit confirmation, explicit calendar title, EventKit apply, and read-back verification.
- `contacts_search`: capped Contacts name/organization metadata search through Contacts.framework.
- `contacts_get`: exact Contact details by opaque `contacts:contact:v1:` handle, capped and read-only.
- `contacts_plan_change`: non-mutating future Contacts contact create preview, with idempotency and approval metadata only.
- `contacts_apply_change`: approved Contacts contact create, requiring a matching plan approval token, explicit confirmation, Contacts.framework apply, and read-back verification.
- `photos_search`: capped Photos original-filename metadata search through PhotoKit.
- `photos_get_asset`: exact Photos asset/resource metadata by opaque `photos:asset:v1:` handle, capped and read-only.
- `photos_export_asset`: exact Photos asset export by opaque `photos:asset:v1:` handle to a caller-selected output directory, read-only.
- `photos_plan_change`: non-mutating future Photos image/video import preview, with source-file hash binding, idempotency, and approval metadata only.
- `photos_apply_change`: approved Photos image/video import, requiring a matching plan approval token, explicit confirmation, PhotoKit apply, and created-asset read-back verification.
- `reminders_search`: capped Reminders title metadata search.
- `reminders_due`: bounded due-window Reminders metadata search.
- `reminders_eventkit_search`: capped Reminders title metadata search through EventKit.
- `reminders_get_content`: exact Reminder notes by opaque `reminders:reminder:eventkit:v1:` handle, capped and read-only.
- `reminders_plan_change`: non-mutating future Reminder create/complete/due-date preview, with idempotency and approval metadata only.
- `reminders_apply_change`: approved Reminder create/complete/due-date apply, requiring a matching plan approval token, explicit confirmation, expected state, EventKit apply, and read-back verification.

## Boundaries

- Never print or persist raw MIME, full headers, Messages transcript text outside exact selected responses, exact Messages attachment metadata outside selected-chat responses, exported Messages attachments outside the caller-selected export path, full Hide My Email aliases outside exact selected responses, Voice Memos transcript text outside exact selected responses, exported Voice Memos audio outside the caller-selected export path, full Safari URLs outside exact selected responses, raw Shortcuts identifiers or shortcut bodies/action graphs, Books annotation text outside exact selected-book responses, raw Books asset IDs, annotation UUIDs, local Books paths, Podcasts transcript text, raw Podcasts identifiers, Podcasts feed/enclosure/web URLs, local Podcasts download paths, Music audio bytes, lyrics, file paths, raw Music identifiers, play history, ratings/favorites, or playlist track dumps, TV video bytes, file paths, artwork, descriptions, raw TV identifiers, playback state, watched state, ratings/favorites, or playlist item dumps, Freeform board BLOBs, decoded board items, board titles/content, assets, previews, collaboration payloads, raw Freeform identifiers, contact details outside exact selected responses, Photos asset/resource metadata outside exact selected responses, exported Photos assets outside the caller-selected export path, exact Mail attachment metadata outside selected-message responses, exported Mail attachments outside the caller-selected export path, exact Notes attachment metadata outside selected-note responses, exported Notes attachments outside the caller-selected export path, Mail/Messages/Notes attachment source media paths, Photos import source paths/hashes outside transient preview/apply responses, reminder notes outside exact selected responses, Reminder plan titles/notes outside transient preview responses, credentials, raw database rows, local Mail file paths, raw Voice Memos source paths, raw Photos identifiers, raw iCloud Drive file paths, raw Hide My Email identifiers, or full account identifiers.
- Mail, Messages, inferred Hide My Email aliases, Voice Memos, Safari, Shortcuts, Books, Podcasts, Music, TV, Freeform, Notes, Calendar, Contacts, Photos, Reminders, and iCloud Drive content/detail may be summarized or quoted only from the exact selected handle response, and should not be copied into durable docs or logs.
- Do not mutate Mail outside the approved create-draft apply path, and do not mutate Notes, Messages, Hide My Email, Gmail, TCC, launchd, Codex config, or OpenClaw runtime state from this skill except through the approved Notes create/append-text apply gate and approved Messages send-text apply gate. Reminders mutation is limited to the approved create/complete/due-date apply path. iCloud Drive mutation is limited to approved create-text under an exact opaque parent folder handle and append-text under an exact opaque file handle plus expected current SHA-256. Calendar mutation is limited to the approved timed-event create path in an explicit target calendar. Contacts mutation is limited to the approved create-contact path through Contacts.framework. Notes mutation is limited to approved create-note apply and append-text apply under an exact opaque note handle plus expected current SHA-256. Photos mutation is limited to approved image/video import apply. Messages mutation is limited to approved send-text to one exact existing chat. Mail send, Mail management, Mail attachment mutation, broad Mail attachment export, Messages direct-recipient send/new-chat/SMS-fallback/file-send/edit/delete/reaction/tapback mutation, Photos edit/delete/album/metadata mutation, and Photos network fetch remain outside scope.
- Do not retry by switching to Gmail plugin access unless the user explicitly asks for Gmail connector behavior.
- Report warning codes and remediation from the tool output when a local store is degraded or unavailable.
