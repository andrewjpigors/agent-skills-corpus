---
name: commit
description: Generate structured Conventional Commits messages for staged changes
disable-model-invocation: true
---

Generate a structured commit message for the currently staged changes. Follow this process:

## 1. Analyze Changes

- Run `git diff --staged` to see what's being committed
- Run `git diff --staged --stat` to see file summary
- If nothing is staged, check `git status` and suggest what to stage

## 2. Classify Change Type

Determine the primary change type using Conventional Commits:

| Type | Description |
|------|-------------|
| feat | New feature or capability |
| fix | Bug fix |
| refactor | Code restructuring without behavior change |
| docs | Documentation only |
| test | Adding or updating tests |
| chore | Build, CI, dependency updates |
| perf | Performance improvement |
| style | Formatting, whitespace (no logic change) |

If changes span multiple types, use the most significant one. If truly mixed, suggest splitting into multiple commits.

## 3. Determine Scope

Identify the affected area:
- Module or package name
- Feature area (auth, api, db)
- File group (config, tests)

Use lowercase, short scope: `feat(auth):`, `fix(api):`, `docs(readme):`

## 4. Write Commit Message

Format:
```
<type>(<scope>): <short description>

<body - optional>

<footer - optional>
```

Rules:
- Subject line: imperative mood, no period, max 72 chars
- Body: wrap at 72 chars, explain WHAT and WHY (not HOW)
- Footer: reference issues (`Closes #123`), note breaking changes (`BREAKING CHANGE: ...`)

## 5. Present for Approval

Show the proposed commit message and ask if the user wants to:
1. Use it as-is
2. Edit it
3. See a different version
4. Split into multiple commits

Do NOT execute the commit automatically. Present the message and wait for approval.
